#!/usr/bin/env bash
# This script groups visitors by IP and HTTP status code, counts occurrences, and displays the data

awk '{print $1, $9}' /path/to/apache-access.log | sort | uniq -c | awk '{print $1, $2, $3}' | sort -nr

